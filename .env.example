# nakari 环境变量

# LLM API (OpenAI-compatible)
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o
# Optional: embedding model for vector search (defaults to OpenAI text-embedding-3-small)
# Set if your provider uses a different embedding model
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small
# Optional: set for non-OpenAI providers (e.g. Zhipu, DeepSeek, etc.)
# OPENAI_BASE_URL=https://open.bigmodel.cn/api/coding/paas/v4

# Neo4j
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=nakari-dev

# Serper.dev (Google Search API)
# Get your API key at: https://serper.dev/api-key
# SERPER_API_KEY=your-serper-api-key-here
# Optional: Custom base URL (default: https://google.serper.dev)
# SERPER_BASE_URL=
# Optional: Request timeout in milliseconds (default: 10000)
# SERPER_TIMEOUT=10000
